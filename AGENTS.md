# AGENTS.md

Guidance for AI coding agents working in this repo — see `CLAUDE.local.md` (gitignored) for the fuller local dev-workflow notes.

Docs-only guidance (the benchmark-number cross-reference map, one per BENCH-XREF-tagged file) lives in [docs/AGENTS.md](docs/AGENTS.md) instead of here — it doesn't apply to source/test work, so keeping it out of this root file keeps every non-docs session from loading it. Touching `docs/`, `README.md`, or `.github/copilot-instructions.md` benchmark numbers? Read that file first.
